<div class="jumbotron center">
	<h1> Welcome! </h1>


	<% @users.each do |user| %>
		<%= link_to user.name, user_path(user) %> 
	<%end%>

	

	<%= link_to 'Login', login_form_path, class: 'btn btn-primary' %>
	<%= link_to 'Sign Up', new_user_path, class: 'btn btn-primary' %>

</div>